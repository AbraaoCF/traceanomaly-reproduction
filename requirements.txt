# Dependências PyPI do ambiente fixado de reprodução, nas versões EXATAS resolvidas
# na imagem funcional (via `pip freeze`). Consumido por docker/Dockerfile.
#
# TensorFlow 1.15 vem da imagem base (tensorflow/tensorflow:1.15.0-py3, Python 3.6).
# As duas dependências que só existem em git são instaladas no Dockerfile:
#   - zhusuan   (do master; resolveu para 0.4.0 no build -- ver ressalva no Dockerfile)
#   - tfsnippet v0.2.0-alpha1 (com --no-deps, para não rebaixar o TF)
numpy==1.16.4
scipy==1.4.1
scikit-learn==0.20.4
pandas==1.1.5
matplotlib==3.3.4
PyYAML==5.4.1
requests==2.27.1
click==8.0.4
fs==2.4.16
imageio==2.15.0
lazy-object-proxy==1.7.1
six==1.11.0
ipython==7.16.3
semver==2.13.0
natsort==8.2.0
tqdm==4.64.1
filelock==3.4.1
mock==5.2.0
idx2numpy==1.2.3
frozendict==2.4.7
